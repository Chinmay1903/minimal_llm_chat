{
  "name": "plumloom-heroku-build",
  "private": true,
  "scripts": {
    "heroku-postbuild": "npm --prefix frontend ci || npm --prefix frontend install --no-audit --no-fund && npm --prefix frontend run build && rm -rf backend/static && mkdir -p backend/static && cp -r frontend/dist/* backend/static/"
  }
}
